<!-- Example feedback file (issue #35). The REAL file lives in your private state
     repo as `feedback.md`, NOT in this public code repo — it holds personal taste
     reactions, so it's kept out of the shared repo (and off every fork). This
     committed `.example` only shows the format. See docs/customizing.md,
     "Providing feedback", and SKILL.md, "Capturing feedback (post-run)".

     Your reactions to past weeks' picks. Append a bullet under a `## YYYY-MM-DD`
     heading; the Friday run reads this (via `bash scripts/feedback.sh read`)
     before searching and steers toward what you liked, away from what you didn't.
     Recent entries (last ~12 weeks) weigh most; older ones are soft context. Keep
     it to taste signal about the picks only — what to hear more or less of, loved
     or disliked, by artist, genre, or scene. Two ways to add to it: edit the file
     in the state repo directly, or react to a run in its Claude session (it opens
     a PR against the state repo for you to merge). -->

## 2026-05-22
- The ambient stuff this week didn't land — pull back on beatless drone.

## 2026-05-29
- Loved the Big Thief-adjacent picks; more of that indie-folk axis, please.

## 2026-06-05
- Bill Orcutt's latest album was great — more solo / exploratory guitar like that.
